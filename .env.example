# 清洗工作流环境配置模板
# 复制为 .env 并填写本机路径后使用(独立 venv 工具, 不污染主环境)
# 与 yt-dlp/ffmpeg 同模式: subprocess 调用独立 venv, 主环境只装轻量 pip 库

# trafilatura venv python(HTML 正文提取, 可选——纯文本场景不需要)
TRAFILATURA_PY=

# presidio venv python(PII 脱敏, 发布前用)
PRESIDIO_PY=

# 输入知识库目录(批量清洗源, 可选——也可用 --input 指定)
CLEAN_KB=
