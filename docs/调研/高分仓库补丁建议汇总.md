===== # 调研报告：modelscope/FunASR（数据清洗视角） =====
## 7. 是否值得

**总评：2.5 星 → 按场景拆分。**

- 作为"清洗工作流本身"引入：**1 星，不值得**。它是转写引擎，不提供去重/OCR/语义纠错，与我们的 core 使命错位。
- 作为"转写源头/上游增强"：**3 星，值得打补丁**。若我们视频转写用的是外部 ASR（或未来要换/二次转写），`funasr -f json` 的分句输出直接根治问题1。
- 作为"思路借鉴"：**2 星**。postprocess_utils 的缩写合并/BPE 合并算法（`sentence_postprocess` + `abbr_dispose`，MIT 可直接抄）可落进我们的 `rules/cleaning_rules.yaml`，用来解决部分问题4。

**决策结论：不整体引入 FunASR，但做两件事——(a) 若转写源已含结构化字段，改清洗层消费结构；(b) 移植其词碎片合并算法到规则层。** 满足"问题1 打补丁，问题2/4 借鉴思路，问题3 不指望"。
## 8. 补丁建议（具体融入方案）

1. **改 clean_batch.py 消费层（问题1，3星补丁）**
   新增 `asr_json_cleaner.py`（或改造现有 `cleaner/` 入口）：输入为 ASR JSON（含 `segments`/`sentence_info`，每项含 `text/start/end`），清洗时**保留分句/分段结构**，逐条过 `clean-text` 归一化 + 自研规则，最后再按段落重组输出。若转写源已支持 `-f json`/`srt`，优先消费结构而非纯文本。所有下游规则（校验/脱敏）改造成逐句批处理。

2. **rules/cleaning_rules.yaml 新增"词碎片合并"规则（问题4，2星借鉴）**
   从 FunASR `funasr/utils/postprocess_utils.py`（MIT）移植 `abbr_dispose` 思路：单字母+空格+单字母 → 合并为缩写大写（"S tla r" → "STla r" 的前缀问题）；`@@` 后缀 BPE 碎片合并。写成 YAML 规则 + Python 实现，挂在现有规则引擎。

3. **段落重复检测（问题2，2星借鉴）**
   不改清洗核心：基于保留的 `start/end` 时间戳 + 文本 n-gram 相似度，在规则层做"同文本 + 时间区间近似重复 = 回放去重"。不引入 FunASR。

4. **可选增强：接入 SenseVoice + ct-punc 做二次转写/补标点**
   若视频无字幕或现有转写质量差，`pip install funasr` + 下载 SenseVoiceSmall（234M，CPU 17x 实时），对分句丢失/无标点的文本用 ct-punc 补分句标点，作为校验层对照输出。成本低、可回退。

5. **引入范围界定**
   - 仅要思路：零引入，只抄算法进规则层（推荐起步）。
   - 要结构化输出：仅加 `funasr` 作为可选转写后端，不并入核心清洗依赖。
   - 不建议：把 FunASR 当作清洗主引擎。

> 数据来源：gh api 真实拉取（仓库元数据、README、目录树、postprocess_utils.py 源码、docs/cli.md、languages）。stars 19754 为拉取时实况。

===== # VideoCaptioner 调研报告（针对"视频转写清洗"） =====
## 7. 是否值得

**结论：混合评级 ≈ 2.5 星——问题4 值得 3 星打补丁，问题1/2 值得 2 星借鉴思路，不整体引入。**

理由：
- **整体引入不划算**：`pip install videocaptioner` 会拖入 PyQt5 / modelscope / GPU 检测等重依赖 + 66MB 资源，与我们"网页/文本/视频转写 → 干净文本"的轻量清洗工作流定位严重不符。
- **GPL-3.0 传染风险**：直接引入整个包作为依赖会把我们的清洗工具拖入 GPL 义务（我们项目是自研独产工具）。但**只借鉴 prompt 文本/算法思路/agent_loop 模式不涉及代码复制**，风险低。
- **真正高价值可搬**：① optimize prompt + agent_loop 校验重试模式（几十行核心逻辑，依赖只需 openai + json-repair）；② ASRDataSeg"片段保留结构"的数据模型思路；③ chunk_merger 的 difflib 模糊重叠去重思路。三者都能以"我们自己写实现"的方式融入，避开 GPL 复制。
## 8. 补丁建议

**补丁A（最高价值，问题4，3星）：LLM 词级纠错 Stage**
- 位置：`cleaner/cleaning.py` 流水线在 Stage3 自研规则之后、Stage4 校验之前新增可选 `Stage3.5 llm_correction`（只处理 ASR/OCR 来源文本，网页 HTML 来源可跳过）。
- 新增 `cleaner/llm_correction.py`：仿 `agent_loop`——输入按句切分的行列表（**保留分句/分段结构，不做全文拼接**，这正是问题1的教训），OpenAI 兼容客户端调 LLM（temperature 0.2），`json-repair` 容错解析，校验"不增删句、不改结构、仅修词级错误"，失败重试≤3轮，再失败保留原文。
- 参照 `optimize/subtitle.md` 重写一版**中文清洗语境** prompt（勿直接复制，规避 GPL）：修词级错误、统一专有名词、去填充词，**明令禁止合并/拆分句子**。
- 规则入 `rules/cleaning_rules.yaml` 新增 `llm_correction` 分组（开关/模型/温度/术语表路径），延续我们"加配置不改代码"的哲学；术语表作为 reference 注入，等价于 VideoCaptioner 的 reference 参数。
- 批量成本：`clean_batch.py` 批量时加 diskcache 缓存（他们正是用 diskcache 缓存 ASR 结果），避免重复调用计费。

**补丁B（问题1，2星）：ASR json 解析保留结构**
- 改我们 ASR 输入解析：把 segments/sentences 从"拼长段"改为"片段列表（文本+起止时间）"，借鉴 `ASRDataSeg` 最小模型；清洗时按时间间隔（如 gap>1.5s）保留段落边界，供补丁A的按句纠错使用。**不引入** LLM 断句（太重），只做时间间隔分组（`_group_by_time_gaps` 思路，纯规则几十行）。

**补丁C（问题2，2星）：模糊段落去重**
- 在现有"整行精确重复≥3次"之上，借鉴 chunk_merger 的 `difflib.SequenceMatcher`（阈值 0.7）加一个**模糊重复检测**：处理"视频反复播同一段但 ASR 文本略有出入"的段落。实现为 cleaner 内一条规则，`cleaning_rules.yaml` 加阈值配置，无新增依赖。

**不引入**：整个 videocaptioner 包、PyQt5 GUI、翻译/TTS/合成模块、LLM 断句。

**GPL 提醒**：只搬 prompt 思路（重写为中文语境）与算法思路、自写实现；直接复制其源码或 prompt 原文需 GPL 兼容考量。

===== # 调研报告：YaoFANGUK/video-subtitle-remover（VSR） =====
## 7. 是否值得

**评分：★☆☆☆☆（1.5 星，介于"借鉴思路"与"不值得"之间，不引入任何代码）**

具体原因：
1. **方向错配**：VSR 解决的是"视频画面里去字幕"，我们解决的是"转写文本里清洗内容"。它输出视频，喂不进 cleaner/ 和 clean_batch.py。
2. **不产出文本**：它只用 OCR 的检测环节（TextDetection 只给坐标框，paddleocr 的识别能力根本没启用），所以连"OCR 文本来源"都算不上，无法对接我们的 rules/cleaning_rules.yaml。
3. **引入成本极高**：CUDA/PaddlePaddle/Torch 数 GB 环境，仅为一个 1~2 星的思路不值得。
4. **真实对口的姊妹仓库是 VSE**：README 自认配合 YaoFANGUK/video-subtitle-extractor 使用——**如果我们的视频转写来源含"硬字幕提取"，应调研 VSE，而非 VSR**。

唯一值得带走的两个"思路"（非代码）：a) 镜头/场景切分辅助段落去重；b) 字幕条区域约束辅助 OCR 只捞有效行。这两个思路用我们自研的轻量手段即可实现，无需引入本仓库。
## 8. 补丁建议

**结论：不引入、不打补丁。** 仅两条思路可借鉴，且用自研手段落地：

| 借鉴思路 | 融入方式 | 改动点 |
|---|---|---|
| 场景/镜头切分 → 辅助段落去重 | 针对问题②，在 ASR 转写清洗前置加"片段边界"信息源：若能拿到视频，用 ffmpeg `select='gt(scene,0.3)'` 或轻量 scenedetect（pip 独立包，无需 VSR 全套）给转写文本按场景打分段标记，再让清洗器按"场景边界 + 文本相似度"合并重复段落 | `clean_batch.py` 加可选 `--scene-split` 前置步骤；`rules/cleaning_rules.yaml` 加"跨段重复"规则 |
| 字幕条区域约束 → 只捞有效字幕行 | 针对问题③，若 OCR 源是视频硬字幕：用 ffmpeg crop 只裁剪底部字幕条再送 OCR（消除全屏碎片）；或对 OCR 结果按"行高/位置/时长驻留"特征打分，教学短行（停留久、位置固定）与 OCR 碎片（闪现、位置随机）区分 | 清洗引擎新增一条"驻留/位置"启发式规则，写入 `rules/cleaning_rules.yaml` |

**不做什么**：不引入 paddleocr 检测、不引入 inpaint、不引入 torch/PaddlePaddle 依赖。

**给用户的一句话建议**：我们的视频转写清洗，文本域问题（①④）VSR 一个都帮不上；图像域问题（②③）它能给的只是"场景切分"和"字幕条约束"两个思路，用 ffmpeg + 现有引擎自研即可。若未来要处理"视频硬字幕提取成文本"，请直接调研姊妹仓库 **YaoFANGUK/video-subtitle-extractor（VSE）**——那才是产出字幕文本、能对接我们清洗链路的工具。

===== # chatdoc-com/OCRFlux 调研报告 =====
## 7. 是否值得（3星直接打补丁/2星借鉴/1星不值得 + 原因）

**评级：2星（借鉴）**

原因：
1. **不能直接打补丁（非 3星）**：OCRFlux 是重资产 VLM 工具——要 NVIDIA GPU、vllm、3B 模型权重、独立 conda 环境。我们的清洗工作流是轻量规则引擎（cleaner/ + rules YAML + clean_batch.py），把整个 GPU 管线塞进来是"杀鸡用牛刀"，且与"网页/文本/视频转写"输入不匹配（它只吃 PDF/图片）。
2. **但值得借鉴三大机制（非 1星）**：
   - **元素编号化处理**（问题 1 主解）：把结构化 JSON 平铺成"编号元素列表"、逐元素判定、再组装——正是我们问题 1 缺的架构思路。
   - **合并检测/合并任务分离**（问题 3 主解）：先判"要不要合"、再执行"怎么合"，两步解耦，可分别做成规则。
   - **覆盖式去重**（问题 2 参考）：重复元素"保留一份、其余置空"，比"删除"更安全，可应用到视频重复段落。
3. 它的 `merge_pages`/`merge_tables` 两个子任务**输入都是纯文本**（不依赖 VLM 图像），说明这套"合并判定"逻辑完全可以脱离大模型、用我们的规则引擎重实现。
## 8. 补丁建议（改哪/加什么/是否引入依赖）

| 建议 | 改哪/加什么 | 是否引入依赖 |
|---|---|---|
| 1. 给 ASR 清洗加"元素列表"中间态 | 在 cleaner/ 与规则之间加一层：ASR json 的 segments/sentences → 编号元素列表（保留原始分句/分段 + 可选时间戳），清洗规则逐元素执行，最后再组装成文本。可先复用 clean_text 规则库做逐元素归一化，另写 `assemble_elements()` 做拼接（参考 OCRFlux build_document_text：中文不插空格、英文补空格） | 无新依赖，纯内部重构 |
| 2. 加"段落重复合并"规则 | 在 rules/cleaning_rules.yaml 增加 `dedup_repeated_block` 类规则：相邻/间隔元素文本相似度（如 difflib.SequenceMatcher ≥ 阈值）→ 保留首份、其余置空（覆盖式，非删除）；配合现有校验环节复查。可加白名单（重复词/字幕重复属正常） | 无新依赖（Python 内置 difflib） |
| 3. 加"碎片判定"规则替代 LLM 判定 | 把问题 3 建模为"元素合并判定"：连续两元素若满足（尾字符为连字符/句中断点 + 下元素以字母/数字开头 或 中文字符无标点结尾）→ 合并为一句；否则视为独立短行。参考 OCRFlux element_merge_detect 的"编号对 + 判定"范式，但用规则实现 | 无新依赖 |
| 4. 词级纠错独立做 | OCRFlux 对问题 4 无贡献，勿因此引 vllm；继续用现有方案（词典/规则/LLM 兜底），与本次调研无关 | 不引入 |
| 5. （可选，远期）借鉴其评测思路 | 若以后要验证合并/去重规则，可仿照 OCRFlux "索引对标注 + F1"方式给样例打标签做回归测试 | 无新依赖 |

**结论**：不建议把 OCRFlux 引入本项目（GPU/VLM 依赖与输入形态都不匹配）；但它"元素编号化 + 合并检测/执行分离 + 覆盖式去重"的架构是问题 1/2/3 的现成蓝本，且不依赖 GPU 即可用规则重实现。依赖引入：**零新增**（仅用 Python 标准库 difflib 与现有规则引擎）。

===== # GitHub 仓库调研报告：gnehs/subtitle-translator-electron =====
## 7. 是否值得

**结论：3星 —— 不打整个包，但「2 个算法 + 1 个工程模式」值得摘进我们的清洗工作流。**

- 值得的点（高内聚、可抽离）：
  1. `compactRepetitiveSubtitleText`（问题2 直接受益，纯函数、10 行核心逻辑）；
  2. checkpoint 断点续传工程模式（源指纹 size+mtime + 配置指纹 sha256 + 增量跳过，解决批处理重跑浪费）；
  3. "保边界、1:1、禁止合并"的处理纪律（问题1 的结构守卫理念）。
- 不值得的点：整个 Electron 应用、字幕解析库、翻译输出层——我们明确不洗 srt，且引入 Electron 技术栈与我们的 Python 管线完全异构。
- 它解决不了我们的问题 3（OCR碎片）和问题 4（词级纠错），这两条要继续走自研规则/校验路线。

---
## 8. 补丁建议（具体融入方式）

**引入方式：纯代码移植，不引入任何新依赖**（算法全部是自包含纯函数）。

1. **【问题2·直接打补丁】新增重复压缩规则**（改 `rules/cleaning_rules.yaml` + 自研规则，不动现有 Stage0-5）：
   - 在 `video_asr`/`video_ocr` 分组新增"精确周期折叠"规则：文本归一化后按行/按句切分，检测长度 1-3 的精确重复周期（≥N 次，N 可配置，示例样本中"重复句 12 段"即此类）；
   - 折叠产物仿照该仓库的标注手法：`原文 … [source phrase repeats N times total]`，既保留信息又不撑爆段落；
   - 与现有"高频重复短行→删水印"规则并存：**短行高频=水印（删），长句周期重复=复读（折叠）**，正好回答"OCR碎片 vs 教学短行"之外的第三种形态。
   - 参照实现：`electron/main/utils/subtitle-chunks.ts` 的 `compactRepetitiveSubtitleText`（Python 移植，白空格/Unicode 归一化已在我们 Stage1 有，tokenize 用 `str.split()` 即可）。

2. **【问题1·借鉴思路】ASR json 处理时保留"条"边界**（改 `clean_batch.py` 的 ASR 分支 / cleaner 的 `video_asr` form）：
   - 解析 json 后以 `segments[]/sentences[]` 为"条"，逐条规整（乱码/标点/空段），**禁止先 `"".join(segments)` 再清洗**——这正是该仓库"每批 5 条 + prompt 禁止合并"的对立面；
   - 若后续引入 LLM 清洗/润色，照抄其 prompt 纪律：`Do not add, remove, split, merge, summarize, explain, or renumber`（1:1 映射 + 边界守卫）。

3. **【工程模式】clean_batch.py 增量续跑**（借鉴 `translation-checkpoint.ts`）：
   - 输出清洗结果时写"源指纹（size+mtime）+ 清洗规则版本 + 哈希"，下次批处理遇到指纹+版本不变的直接跳过——批量清洗不重复跑已洗文件。

4. **【可选·不引入】LLM 输出重复防护**：若未来清洗链路接入 LLM（如 ASR 纠错），移植 `TranslationOutputRepetitionGuard`（KMP 前缀函数，Python 30 行内可实现），防模型复读死循环。

5. **明确不引入**：Electron 应用本体、subtitle/ass-parser 库、翻译输出层、srt 相关一切。

**改动面最小化**：只动 `rules/cleaning_rules.yaml`（新增规则分组）与 `cleaner/cleaning.py`（加 1-2 个自研函数 + form 路由），零新依赖，不影响现有知乎/HTML 清洗链路。

===== # google/langextract 调研报告 =====
## 7. 是否值得（3星直接打补丁/2星借鉴/1星不值得 + 原因）

**评分：⭐⭐ 借鉴思路**

**原因**：
1. **方向错位**：我们是"清洗全部文本成干净全文"，它是"LLM 抽取指定实体"。直接拿来当 cleaner 补丁等于把确定性流水线换成 LLM 抽取 + 模型成本，违背我们"离线、零成本、可审计规则"的架构。
2. **引入新依赖与成本**：要发挥它就得引 Gemini/OpenAI API Key 或 Ollama，把"无 LLM 的规则引擎"升级成"有 LLM 的抽取管线"，与 .env 里现有工具 venv 隔离模式（trafilatura/presidio 独立 venv）冲突不大但成本可观。
3. **但有 3 个高价值算法思想值得移植**（不引入依赖，手写即可）：
   - **LCS 模糊对齐**（问题4）：resolver.py 里 `_lcs_fuzzy_align_extraction`，阈值 0.75、密度 1/3、支持 diff——可直接改造成我们"ASR 错词↔参考词"的对齐/纠错规则。
   - **句子级分块 + 区间保留**（问题1）：清洗 video_asr 时先按 segments/sentences 切块再清洗，保持分句/分段，而不是先拼成一长段再切。
   - **重叠合并 first-pass wins**（问题2）：模糊去重的"保留首次出现、后续近似段丢弃"策略，比当前"精确重复才去重"更能抗 ASR 噪点。

---
## 8. 补丁建议（改哪/加什么/是否引入依赖）

**建议：不引入 langextract 依赖，只移植其 3 个算法思想到我们的规则引擎。**

| 动作 | 位置 | 说明 |
|------|------|------|
| **① 移植 LCS 模糊对齐**（针对问题4 词级纠错） | `cleaner/cleaning.py` 或 `cleaner/` 新增 `fuzzy_align.py` | 参考 resolver.py 的 LCS 算法（阈值 0.75、密度 1/3）手写一个纯 Python 实现：给定"ASR 错词序列"与"候选正确词序列"，算最佳对齐 span 与匹配度，作为 `video_asr` 组规则的词级纠错/降噪依据。**零新依赖**（difflib 标准库即可） |
| **② 保留 segments/sentences 结构**（针对问题1） | `rules/cleaning_rules.yaml` 的 `video_asr` 组 + clean_batch 输入层 | 清洗前按 json 的 `segments[].text` 逐条清洗、再按 `sentences` 分组输出，而不是 `json.dumps(text)` 拼长段后再洗。让"分句边界"作为清洗的输入维度而非输出后丢失 |
| **③ 模糊段落去重**（针对问题2） | `rules/cleaning_rules.yaml` 重复句规则升级 | 现"精确重复才删"→ 加"LCS 相似度 ≥0.75 的跨段重复"识别（视频反复播同一段时 ASR 有噪点，精确匹配会漏），删除策略用 first-pass wins（保留首次出现） |
| **④ 不引入依赖** | 全流程 | langextract 本身（Gemini/OpenAI/Ollama 调用）**不引入**；保持纯规则、零 API Key、离线可审计。若将来要"LLM 智能清洗"再单独评估，且建议走我们现有的 GLM 桥接（VLM-http-client 模式）而非新引 langextract |

**风险提示**：LCS 模糊对齐是 O(n·m) 时间/空间，对长段要做窗口化（参考其 min_density 与 span 上限）防止批量清洗变慢；去重阈值 0.75 需用我们真实 ASR 样本标定，防止误删教学短行（问题3 不受此库帮助，仍靠我们 `video_ocr` 组的高频重复+行模式规则）。

===== # jitsi/jiwer 调研报告 =====
## 7. 是否值得（3星直接打补丁/2星借鉴/1星不值得 + 原因）
**评级：2星（借鉴，不直接打补丁）**

原因：
- jiwer 定位是**语音识别评测库**，与我们"把转写清洗成干净文本"的目标错位 —— 它不给清洗结果，只给相似度与对齐。直接引入做清洗管道主体不成立。
- 但它背后的两块算法能力值得**借鉴**：
  1. **RapidFuzz 词级对齐**（`process_words` 的 AlignmentChunk）→ 是我们问题 4 最缺的"词级错误精确定位"基础设施；
  2. **WER/相似度** → 是问题 2（段落重复）最直观的量化判据。
- 复用成本极低：它是纯 Python 小库，`pip install jiwer` 即得，Apache-2.0 协议无引入障碍；即使不引库，底层 RapidFuzz 也可单独 pip 引入。
- 不直接打补丁的理由：① 它的 Transform 与 clean-text/自研规则大量重复；② 纠错/去重的业务逻辑 jiwer 不提供，仍需自研；③ 引入新依赖收益不如"抽 RapidFuzz 对齐思想 + 自研规则"大。
## 8. 补丁建议
| 位置 | 动作 | 是否引入依赖 |
|---|---|---|
| `cleaner/`（新模块，如 `cleaner/asr_align.py`） | 借鉴 jiwer 对齐模型：用 RapidFuzz 对"参考文本 ↔ 待纠错文本"做词级对齐，输出 AlignmentChunk 同款结构（equal/substitute/insert/delete + 双向索引），供纠错与质检使用 | 可选：`pip install rapidfuzz`（C++ 快，单依赖）；或 `pip install jiwer` 直接用其 `process_words` + `visualize_alignment`，两者均可 |
| `rules/cleaning_rules.yaml` | 新增"ASR 常见错词纠错表"，借鉴 `SubstituteWords` 的 `\b` 整词匹配语义（防子串误替换，如 `foo` 不误伤 `foobar`），映射 `S tla r→Stapler`、`acc commodation→accommodation` 等 | 否（纯 YAML + 正则） |
| 去重逻辑（`clean_batch.py` 或自研规则） | 对相邻 segment 用 WER/相似度做重复检测：`jiwer.wer(a,b)`（v4 空串安全），低于阈值判重复段删除 | 若已引 rapidfuzz/jiwer 则复用，否则用 difflib.SequenceMatcher 即可，不引依赖 |
| 质检模块（`cleaner/` 校验层） | 引入 `collect_error_counts` + `visualize_alignment` 做清洗前后对比报告：清洗前 vs 清洗后对同一批转写算 WER/CER，量化改进；可视化对齐供人工抽查 | 可选引入 jiwer（最省事） |
| 不采纳的部分 | ① 不把 jiwer 当清洗主引擎（它不产出干净文本）；② 不用其 Text 归一化 Transform（与 clean-text/自研规则重叠）；③ 不解决问题 1、3（需自研分段保留策略与短行判别规则） | — |
| 依赖总体建议 | 最低成本路径：**只 `pip install rapidfuzz`** 一个 C++ 距离库，自研对齐函数（jiwer 的对齐封装仅 ~100 行）；若想快速验证效果，直接 `pip install jiwer` 试用 `process_words`/`visualize_alignment`，再决定是否抽出。两步都不引入重型框架 | 见上 |

===== # WhisperJAV 调研报告（针对「视频转写清洗」融合评估） =====
## 7. 是否值得

**结论：值得，分级处理——问题 1、2 直接打补丁（3星），问题 3、4 借鉴思路（2星），不整体引入。**

理由：
- 这是全网少见的**"专门对抗 ASR 幻觉/重复"的后清洗系统**，与"视频转写清洗"目标完全重合；而且它的清洗器是**纯 Python + 可摘取 + MIT**，不像多数 ASR 项目清洗逻辑埋在 torch 里。
- 问题 1、2 是它的主战场（README 通篇在讲 repetition hallucination），代码成熟度极高（8 条正则经过 issue 迭代打磨、有回归测试基线）。这比我们自研规则从零撞坑划算得多。
- 问题 3、4 它没有现成方案（无 OCR、无英文词级纠错），但它的"置信度门控""纯符号残渣丢弃""审计台账"三个模式对我们是现成的方法论模板。
- 不整体引入的原因：ASR 引擎、语音增强、GUI 都不是我们职责，引入 torch 全家桶违背我们轻量 cleaner 的定位；也避免与 trafilatura/clean-text 现有链路互相污染。

---
## 8. 补丁建议（具体融入我们的清洗工作流）

**只摘代码，不引包**。把清洗器当"算法移植"，落点：

1. **`cleaner/` 新增两个 stage**（对应问题2）：
   - `repetition_cleaner.py` → 改造成 `cleaner/repetition_dedup.py`：保留三层（规则正则层/通用子串层/长度截断层），**正则全部换成语料驱动的中文/英文规则**（原版是日语 `\p{ぁ-ん}`；中文可用"吧、啊、呢"类语气词短语重复 + 通用 `\p{L}`）。保留 modifications 审计返回结构，对接我们已有的校验日志。
   - **新增第四层：跨段近重复去重**（原版没有，是我们问题2的增量）——按句切块后用 difflib 相似度窗口标记"同一内容重复 N 遍"，只保留首现或摘要版。
2. **`cleaner/` 新增结构保真处理**（对应问题1）：把"逐段清洗"从理念落成代码——`clean_batch.py` 里对 json 的 segments/sentences 改为**逐段映射清洗**（保留 id/起止时间/段落边界，不再先拼长串再洗），并移植 `japanese_postprocessor` 的重分组表换成中文边界（句末：啊/吧/呢/吗/了；标点；时长>8s 拆行）。
3. **`rules/cleaning_rules.yaml` 扩展**（对应问题3、4）：
   - 新增"词级纠错"节：`S tla r`→`star` 类词内空格重接正则（`\b([a-z])\s([a-z]{2,})\b` 组合白名单）、常见 OCR 混淆对（l/1、O/0、rn/m）、领域术语表；
   - 新增"碎片判别"节：纯符号/纯标点/无实质字符行丢弃（移植 `_HAS_LINGUISTIC_CONTENT` 思路，加我们中文/英文的实质字符集），短行+低置信度标记待人工校验。
4. **校验/审计对接**：沿用他们的 `modifications`（type/pattern/confidence/category）结构，统一写入我们现有校验层，保证"清洗可追溯、可回归对比"。

**不引入**：ASR 引擎、VAD/场景检测、语音增强、GUI、SRT 输出链路；`whisperjav` 整体不装，只拷贝上述 4 个纯 Python 模块并改规则表（MIT 许可合规，注意保留版权头）。预计改动量：新增 2 个 cleaner 文件 + clean_batch 逐段化改造 + yaml 规则扩展，属一次中小型补丁。

===== # ploomber/ploomber 调研报告 =====
## 7. 是否值得（3星直接打补丁/2星借鉴/1星不值得 + 原因）

**判定：2 星（仅借鉴其"管线编排"思想，不引入依赖）。**

原因：
- 它是 pipeline 编排器，不是文本清洗工具——对我们最痛的 4 个清洗问题（ASR 结构化、段落去重、OCR 行判别、词级纠错）**零直接贡献**，1 星不值得的理由充分。
- 但它 2 星有两点真实价值：
  1. **中间产物/增量缓存思想**：我们 clean_batch.py 目前可能是"全量重洗"，引入"每份产出可校验 + 变更才重洗"能让批量清洗可追溯、出错可定位到具体清洗阶段；
  2. **YAML 声明式多阶段管线**：与我们 rules/cleaning_rules.yaml 天然同理念，可把"原始→重建分句→去重→判别→纠错→校验→脱敏"写成显式阶段链，规则文件与代码解耦。
- 成本权衡：为这两点引入一个 ~169 万行 Python 的编排框架到"独立 EXE 本地工具"，**严重过度设计**（部署/依赖体积/学习成本都不匹配）。借鉴思想、自写几十行轻量编排即可。
## 8. 补丁建议（改哪/加什么/是否引入依赖）

**不引入依赖。** 借鉴思想，在现有清洗工作流内做轻量改进：

1. **改 `clean_batch.py`：加"变更检测 + 中间产物"轻量缓存**
   - 在批量入口记录每份产物的 `(输入文件hash, 清洗器版本, 规则文件hash) → 产物hash` 到一个小 JSON/SQLite 状态文件；
   - 重跑时仅对"输入变了 / 清洗器或规则变了"的文件重洗，跳过未变文件。成本 ~30 行，零新依赖（用内置 hashlib/sqlite3/json）。

2. **改 `rules/cleaning_rules.yaml`：结构化阶段声明**
   - 借鉴 ploomber 的 spec 思路，把 YAML 从"扁平规则"升级为**分阶段配置**，每阶段一个处理单元 + 可配参数 + 输出名（如 `asr_rebuild / dedup / line_classify / word_fix / validate / desensitize`），让 `cleaner/` 的引擎按阶段表驱动，而非硬编码调用顺序。这直接服务于问题 1（分段重构作为独立阶段）、2（去重阶段）、3（行判别阶段）、4（纠错阶段）的落地组织。

3. **改 `cleaner/`：新增"产物校验"步骤**
   - 每个清洗阶段输出后做轻量断言（非空、段落数合理、无连续重复句、无高频错误 token），失败即中断并报"哪一阶段产出异常"——即 ploomber 中间产物校验的简化版。

4. **不加的东西**：不引入 ploomber 本体、不用 DAG/notebook/soopervisor/云部署；本地工具保持零重依赖。

**一句话补丁总结**：Ploomber 告诉我们"把清洗链显式化、把中间产物可追踪、把变更增量重算"——用 ~50 行自研逻辑把这三点搬进现有 `clean_batch.py` + `cleaning_rules.yaml` 即可，问题 1-4 的**组织框架**有了，算法本身仍靠我们自研（分句/去重/判别/纠错）。

===== # python-validators/validators 调研报告 =====
## 7. 是否值得（3星直接打补丁/2星借鉴/1星不值得 + 原因）

**2星：借鉴，不值得直接引入。**

- 它**不解决任何 4 个目标问题**，作为清洗依赖引入是负收益（多一个包，零功能增益）。
- 但有两个可**借鉴/轻量复用**的点，都是零成本、符合现有「校验」层定位：
  1. **`validator` 装饰器 + `ValidationError` 的注册模式**：我们的自研规则层（rules/cleaning_rules.yaml）本质是"函数+描述"的注册表，与它的装饰器模式同构。照抄该模式能把 YAML 规则与 Python 实现更干净地绑定，且不引依赖。
  2. **`validators.url` / `validators.email` 可作输出质检**：clean_batch 清洗后若想确认"文章里 URL 没被规则误删/邮箱还合法"，直接调这俩函数即可，比自写正则省事。若不想引依赖，仅作参考也可。
- 结论：**不引入依赖**，吸收"装饰器注册 + 校验函数封装"思路进现有校验层即可。
## 8. 补丁建议（改哪/加什么/是否引入依赖）

| 项 | 建议 |
|------|------|
| 是否引入依赖 | ❌ **不引入**。四个目标问题它一个都不解决，引入纯属增加包体积与维护面 |
| 改哪（可借鉴，非必须） | `cleaner/` 的自研校验层：模仿其 `validator` 装饰器，把校验函数与规则名/描述绑定（装饰器返回 `(fn, desc)` 元组，供 rules/cleaning_rules.yaml 按名索引），替代现在的裸函数调用，让"校验"与"规则配置"一一对应 |
| 加什么（可选轻量复用） | 在「校验/脱敏」层加一个"产出物 URL/邮箱保全校验"：清洗后遍历文本，凡能通过 `validators.url/email` 的片段视为合法链接/邮箱，跳过删除类规则，防止把教学素材里的链接误伤。若不愿引依赖，用现有正则等价实现（工作量约半天） |
| 风险 | 无。纯借鉴代码风格 + 可选校验函数，均为 0 依赖改动；即便选择 `pip install validators`，MIT + 零依赖也不污染环境 |

**一句话总结**：validators 是好用的"格式检查工具箱"，但它是**判断题不是清洗题**——对我们 4 个视频转写清洗问题无能为力，顶多借鉴它的装饰器注册模式 + 顺带复用 URL/邮箱校验做产出质检，不必引依赖。

===== # 调研报告：QwenLM/Qwen3-ASR =====
## 7. 是否值得

**结论：值得（3 星，直接打补丁）——但要正确定位：它替换的是"转写来源"，不是清洗引擎。**

原因：
1. 我们项目已深绑百炼 DashScope（双引擎/视频转写），Qwen3-ASR 就是 Qwen 官方产品，**接入路径最短、无新供应商风险**（GitHub 实时确认其 API 托管在阿里云百炼）。
2. 它同时命中问题 1（标点天然保留 + 时间戳）、2（时间戳使能去重）、4（WER 源头碾压），三个问题的补丁都建立在"时间戳 + 高质量文本"这两个它提供的能力上。
3. 不改动我们 cleaner/ 主体架构——只在上游加一个适配器 + 下游加几条时间戳感知规则，符合"最小改动、规则驱动"的既有风格。
4. 唯一的"不值得"场景：如果我们没有 GPU 也不想付 API 费、且现有 Whisper/FunASR 已够用——那问题 1/4 仍存在，说明现有引擎确实差一档，结论不变。

打分里"2 星"的两条（问题2/3）不是它不行，而是它**只提供信号不提供成品**——去重规则和音画融合规则本来就是我们 cleaner 该干的活。

---
## 8. 补丁建议（具体融入我们的清洗工作流）

**总体策略：引入 `qwen-asr` 作为视频转写上游引擎（本地 GPU 或 DashScope API 二选一），并在 cleaner/ 里新增"时间戳感知"清洗层；不 fork 仓库、不引入它的后端服务代码。**

具体动作（对照 cleaner/ 现有结构）：

1. **新增 `cleaner/adapters/asr_qwen3.py`（转写适配器）**
   - 两种模式：`mode=api` 走 DashScope FileTrans（复用我们已有的百炼凭证，零 GPU）；`mode=local` 走 `qwen-asr` transformers 后端（需 GPU，批量离线更快）。
   - 输出统一为结构化转写：`{language, text, time_stamps:[{text,start,end}]}`，供下游规则消费。

2. **新增 `cleaner/sentence_boundary.py`（解决问题1）**
   - 输入 ASR JSON：不再把 segments/sentences 拼成一长段，而是用 ForcedAligner 的词级时间戳按"静音 gap > 阈值 或 句末标点"切出分句/分段。
   - 由于 Qwen3-ASR 文本自带标点，优先用标点分句，时间戳只做段落边界锚定——两者互补，避免又拼成长段。

3. **扩展 `rules/cleaning_rules.yaml`（新增 asr 段落键）**
   - `asr_qwen3.sentence_boundary: use_timestamps|by_punctuation|both`
   - `asr_qwen3.dedup_paragraph_similarity: 0.95`（归一化后相似度阈值）
   - `asr_qwen3.dedup_require_timestamp_overlap: true`（重播段必须时间戳不重叠，防误删正常复读）
   - `short_line_speech_check: enabled`（OCR 短行融合规则开关）

4. **新增 `cleaner/dedup.py`（解决问题2）**
   - 按时间戳把文本切成"段"，对相邻/跨区段做归一化（去标点/空格/大小写）后算相似度（SequenceMatcher 或快速指纹），相似度超阈值且时间戳无重叠 → 判为视频重播段并合并/删除。
   - 把仓库内置的 `detect_and_fix_repetitions`（字符连续重复>20 与短模式重复）移植成一条自研规则，加进既有规则链兜底。

5. **新增 OCR-音频融合规则（解决问题3）**
   - 在自研规则层加一步：OCR 短行（如 1-5 字）若其所在时间窗在 ASR 时间戳中有语音覆盖 → 保留为教学内容；无语音覆盖且高重复 → 判为 OCR 碎片丢弃。这是对"教学短行 vs 碎片"的判别器。

6. **问题4（词级错误）**
   - 主要靠换上游引擎（Qwen3-ASR WER 源头降错）+ 我们已有 normalize/脱敏规则兜底；无需新增大模块。
   - 可选：把 ASR 置信度低片段标记为"待校验"，由现有校验层二次确认。

**是否引入依赖**：
- 引入：`pip install qwen-asr`（仅当走本地 GPU 模式）；或仅加 DashScope API 调用（复用现有 `dashscope_

===== # ropensci/pdftools 调研报告 =====
## 7. 是否值得（3星直接打补丁/2星借鉴/1星不值得 + 原因）

**评级：2 星（借鉴，不引入依赖）**

原因：
1. **不引入 pdftools 依赖**：它是 R 包，我们的工作流是 Python，跨语言调用 R 属于重型依赖，性价比低。
2. **核心价值是设计思想，不是代码**：唯一对我们有实质启发的两点——① 多粒度输出并存（整段 + 逐条结构化）；② OCR 词级置信度先行。这两点我们在 Python 生态里有对等甚至更优实现（`pdfplumber`/`pymupdf` 做 PDF，`rapidocr`/`paddleocr` 自带置信度，`faster-whisper` 的 segments 自带结构化）。
3. **场景不匹配**：PDF 提取≠网页/视频转写清洗，问题2（重复段落）完全无法借鉴。
4. 若未来我们真的需要处理"简历 PDF / 招聘 JD PDF"（BossJobAI 场景潜在延伸），届时可直接选 Python 的 `pymupdf`，而不是绕道 R。
## 8. 补丁建议（改哪/加什么/是否引入依赖）

**不引入 pdftools 依赖。** 按其设计思想在我们的 Python 清洗工作流里打等价补丁：

1. **问题1（结构化丢失）→ cleaner 加"结构化输出通道"**：在 `clean_batch.py` 或 cleaner 中新增 `structured=True` 模式，输出保留 ASR 原始分段边界（`{index, start, end, text}`），清洗后仍按段输出而非 `" ".join` 压平；无 ASR 结构时才退回整段拼合。对应 pdftools `pdf_text`(整段) vs `pdf_data`(逐条) 双粒度。
2. **问题3/4（OCR 碎片与词级错误）→ 规则层加置信度与聚类**：自研规则 `rules/cleaning_rules.yaml` 新增两类规则——a) 短行聚类：对 OCR 词级 `conf < 阈值` 的连续短行按版面距离合并，区分"碎片"与"教学短行"；b) 词级纠错：`conf` 低于阈值的词进词典/语音混淆表查纠（S tla r→Stolar, acc commodation→accommodation）。词级置信度取自已有的 OCR/ASR 输出，无需新依赖。
3. **问题2（段落重复）→ 已有规则增强**：在自研规则里加"重复段落指纹判重"（段落 hash / 相似度阈值），属工作流既有能力延伸，不新增依赖。
4. **校验模块**：若后续真接 PDF（简历/JD），在 Python 侧引入 `pymupdf`（轻量、无系统级依赖，比 Poppler 系好装），并复用上述结构化/置信度管线——这是"未来若需"的备选，非本次动作。

依赖结论：本轮零新增依赖；跨语言 R 包一律不引入。

===== # 调研报告：SYSTRAN/faster-whisper（视频转写清洗专题） =====
## 7. 是否值得：**3星——直接打补丁**

**理由**：
1. 问题 1（结构化丢失）的根因在 ASR 输出格式，cleaner 里再怎么拼规则都只能"事后拆段"，而 faster-whisper 从源头就给你 `segments` + `words`，一步到位且免费（MIT、本地跑）。
2. 问题 4 的根因也在 ASR——它给更准的模型 + 词级置信度，直接降低词级错误率并提供标注信号。
3. 集成成本低：CPU int8 无需 GPU，small 模型即可，`pip install` 一条命令；与我们现有 `video_asr` 形态完全同构，cleaner 只需多接一个 json 格式。
4. 不解决的部分（2/3）本来就是 cleaner 的责任，交给现有规则引擎即可。

**注意**：它是"上游补丁"而非"清洗补丁"——所以要引入的是"转写引擎 + 结构化 json 消费"，cleaner 本身改动较小。若你们只打算清洗已有 json、不打算自己转写，那它的价值就降到 2星（只借鉴 segment 结构思路）。基于我们"视频转写清洗"的完整目标，判 3 星。

---
## 8. 补丁建议（具体融入我们的清洗工作流）

### 8.1 加一个转写子模块（新文件，不改 cleaner 核心）
在 `本地调研项目/清洗工作流/` 下新增 `transcribe/` 目录，封装 faster-whisper：

```
transcribe/
├── transcribe.py     # 封装: 输入音视频 → 结构化 json
├── __init__.py
└── requirements-extra.txt   # faster-whisper 依赖（与原 requirements.txt 分离，可选安装）
```

`transcribe.py` 核心逻辑（建议）：
```python
from faster_whisper import WhisperModel

# CPU int8 起步；有 GPU 再切 float16
model = WhisperModel("small", device="cpu", compute_type="int8")

segments, info = model.transcribe(
    "video.mp4",
    beam_size=5,
    word_timestamps=True,          # ← 关键: 词级时间戳+置信度
    vad_filter=True,               # ← 清静音
    condition_on_previous_text=False,  # ← 抑制幻觉复读(问题4辅助)
)
# 输出结构化 json: {language, segments:[{start,end,text, words:[{word,probability}]}]}
```
产出 json 结构对齐我们现有 `英文测试.json` 形态，但**保留 segments 与 words**，不 merge 成长段。

### 8.2 cleaner 侧小改（改 `cleaner/cleaning.py` + `rules/cleaning_rules.yaml`）

1. **问题 1（保结构）**：`clean_text` 新增形态 `video_asr_segmented`——逐 segment 走现有 Stage1 归一化（标点乱码/连续标点），**清洗后按 segment 用 `\n` 拼接输出**，不再糊成一整段。句界 = 转写自带，规则只做归一化，不做重排。
2. **问题 4（词级错误）**：读 segment.words 的 `probability`，对 `<0.6` 的低置信度词打标（写进 json 的 `low_conf: [...]`），交给现有/新增的纠错层（词典 or 自研规则 or LLM）。用 faster-whisper 的 small/large-v3 后这类错误本就变少，纠错压力骤降。
3. **问题 2（重复段）**：在 cleaning_rules.yaml 加 `video_asr` 的**段级去重规则**——用现有"高频重复行"思路升级为"重复 segment 去重（text 相似度≥阈值且时间间隔近）"，faster-whisper 的时间戳让"视频反复播同一段"可识别。
4. **问题 3（OCR 碎片 vs 教学短行）**：用转写的音频词表做"真值集合"——cleaner 在 `video_ocr` 形态里新增一条规则：OCR 文本若**不含**音频转写词表中的

===== # tenzir/tenzir 调研报告 =====
## 7. 是否值得（3星直接打补丁/2星借鉴/1星不值得 + 原因）

**评级：2 星（借鉴设计思想，不引入依赖）**

原因：
- **领域完全错配**：Tenzir 是安全遥测管线引擎（C++，~11MB 代码，20+ 安全向插件），与"网页/文本/视频转写清洗成干净文本"零交集，3 星"直接打补丁"不可能。
- **但架构思想对问题1-2 有高价值参考**：① 它以"带 schema 的结构化事件"贯穿全流程、绝不把结构化数据压平成字符串——直接反衬我们"json segments/sentences 拼成一长段"的反模式；② 它把清洗/整形固化为**可组合的声明式算子链**，比我们当前过程式 clean_batch.py 更利于复用、回放、验证；③ `deduplicate` 算子证明"去重是管线内建能力"。
- **不值得集成**：引入 = 部署重型 C++ 引擎 + 安全领域概念 + 编译型插件维护成本，对 4 个实际问题无任何直接贡献。只读源码/文档"抄设计"，不装依赖。
## 8. 补丁建议（改哪/加什么/是否引入依赖）

**不引入任何依赖。** 仅借鉴其三条设计思想，落地到我们现有 Python 清洗工作流：

1. **事件化改造 ASR 清洗（解决问题1）**：参考 Tenzir"事件保真"模型，改造 cleaner/ 的 ASR 分支——把 json 的 `segments/sentences` 当作**结构化事件序列**处理，每个 segment 保留 `start/end/序号` 作为元数据，仅在输出阶段按需拼接为段落；禁止在中间环节 `''.join` 成一长串。可对照 Tenzir 的 `normalize` 算子写法，在 `rules/cleaning_rules.yaml` 中为 ASR 结构增加"保留边界"规则类型。

2. **把去重提升为内建算子（解决问题2）**：Tenzir 把 `deduplicate` 做成管线内建算子，而非业务代码里临时写一段。建议在 clean_batch.py 的清洗流程中抽出一个独立的 `DedupParagraphs` 算子（n-gram/Jaccard 相似度去重，含"视频反复播同一段"的间隔重复场景），并与现有校验/脱敏环节一样做成可配置、可单独验证的单元。

3. **管线化重构（通用收益）**：把 clean_batch.py 从"一串函数调用"重构为**声明式算子链**（`[extract_body | normalize | rule_clean | validate | dedup | desensitize]`，定义可写在 yaml），并仿 Tenzir 支持"同一管线对批量文件回放、可单独测试"。此为结构性改造，建议小步走：先抽算子接口，再逐步迁移。

4. **明确不抄的部分**：C++ 引擎、schema/遥测归一化、插件编译体系、存储/查询层一律不引入——它们解决的安全场景与文本清洗无关，只读不搬。

===== # transcriptionstream/transcriptionstream 调研报告 =====
## 7. 是否值得（3星直接打补丁/2星借鉴/1星不值得 + 原因）
**评级：2星（借鉴，不直接打补丁）**

原因：
- **层级错位**：它是「音频→转写+摘要」的上游自托管服务（GPU 重型 Docker 栈），我们是「转写文本→干净文本」的下游清洗工作流。把它整包引入既超重（26GB 镜像、CUDA 依赖、GPL-3.0 传染）又解决不了核心清洗问题（去重/纠错/短行判别它一概没有）。
- 直接打补丁不成立：① 依赖无法塞进轻量 cleaner；② GPL-3.0 抄代码会传染；③ 4 个问题里它只在问题 1 有正面价值。
- 但**算法值得借鉴**：`helpers.py` 里「词流→句子→说话人段落」的重组逻辑（`get_realigned_ws_mapping_with_punctuation` + `get_sentences_speaker_mapping` + `get_speaker_aware_transcript`）是解决「ASR 输出变一长段」的现成参考实现，读源码抄思路（不抄代码，规避 GPL）成本极低，对问题 1 收益直接。
- **附带收获**：.srt 句子级字幕 + LLM 结构化摘要模板，可作为我们清洗产物的输出规范参考。
## 8. 补丁建议
| 位置 | 动作 | 是否引入依赖 |
|---|---|---|
| `cleaner/`（新模块，如 `cleaner/asr_structure.py`） | **借鉴其句子重组思想（不抄代码）**：解析 ASR JSON 的 segments/sentences 时，按「每段自带时间戳 + 句末标点 + 说话人标签（若有）」重建句子与段落，输出保留分段的清洗文本，替代现有 `"".join` 扁平拼接。可参考其「句子最长长度/标点断句」启发式 | 否（纯自研，读其 helpers.py 抄思路） |
| `rules/cleaning_rules.yaml` | 新增「ASR 输出格式还原」规则类别：遇到 segments 无标点拼接时，按时间戳间隙（gap > 阈值）断段、按句号/问号/感叹号断句；可借鉴其 `is_acronym` 逻辑（U.S.A. 不误断句） | 否 |
| 输出层（`clean_batch.py` 或新输出器） | 借鉴其**双格式输出**：清洗后同时输出「分段落 .txt」+「句子级时间戳 .srt（或 Markdown 分段）」，供视频转写场景时间对齐使用 | 否 |
| 清洗后摘要（可选） | 借鉴其 LLM 摘要模板（Speakers/Topics/Ideas/Dates/Locations/Action Items + Summary）做清洗后结构化摘要输出，需自接 LLM 服务 | 可选（已有三方 LLM 路由则可直接复用，不新增） |
| 不采纳的部分 | ① 不引 ts-gpu 整套 Docker 转写服务（GPU/26GB/GPL-3.0 三重不匹配）；② 不做说话人分离（超出清洗范围）；③ 不引入 Meilisearch（与清洗本体无关）；④ 不复制其代码（GPL-3.0 传染风险，只借鉴算法） | — |
| 依赖总体建议 | **零依赖**。问题 1 的修复用自研规则即可完成，仅需读其 `helpers.py`（whisper-diarization fork 内）的句子重组逻辑作为思路参照。若后续要「清洗后摘要」再用现有 LLM 路由，不新增任何 Python 依赖 | 见上 |

===== # 调研报告：YaoFANGUK/video-subtitle-extractor（star 9319） =====
## 7. 是否值得

**整体评级：★★☆☆☆（借鉴思路），其中 P4/P2 的算法可作 ★★★ 局部补丁。**

理由：
- 它是"视频硬字幕提取器"，我们不做 OCR/抽帧，90% 的代码（PaddleOCR 流水线、VideoSubFinder、GUI、多进程）对我们无用，引进来是负担不是资产。
- 真正值钱的是两小块纯算法：
  - **`reformat.py`（约 200 行）**：typoMap 正则纠错 + wordsegment 英文切分 + 标点空格归一化，几乎零依赖（wordsegment + pysrt + re），且它解决的正是我们 P4。→ 值得 3 星直接抄。
  - **`_remove_duplicate_subtitle`（约 60 行）**：Levenshtein 连续相似合并 + 保留最长，逻辑独立可摘，解决 P2 的一半。→ 值得 3 星抄，但要扩展成"全文档重复检测"。
- P1、P3 它没解决或解法不适用，别指望。
## 8. 补丁建议（怎么融入我们的清洗工作流）

**不引入整个仓库**（不 clone、不装 PaddlePaddle），只移植两块算法并适配我们的规则体系：

1. **新增规则源 `rules/typo_map.yaml`（抄 typoMap.json 机制）**
   - 迁移 `configs/typoMap.json` 的"正则 key→替换 value"模式为 YAML：`rules/cleaning_rules.yaml` 追加 `typo_fix` 段，支持"正则替换"与"整词删除"两种动作（如删除"性感荷官在线发牌"）。
   - 关键：把原代码的 `re.sub(k, v, text, re.I)` 落到我们规则引擎的校验/纠错阶段，并在 `clean_batch.py` 支持按源（ASR 转写/OCR）加载不同 typo 表。

2. **移植英文单词纠错器 `cleaner/word_repair.py`（抄 reformat.py 核心）**
   - 引入 `wordsegment` 依赖（轻量、纯 Python），实现：粘连词切分 + 动词缩略形表（I'm/you're/can't…）+ `remove_invalid_segment` 片段合法性过滤。
   - 可同时处理"缺空格粘连"（Let'sqo）与"多空格碎词"（S tla r）两类 P4 错误——后者用 wordsegment 对词内空格变体打分还原，比原代码更强。

3. **移植标点/空格归一化规则集（reformat.py 的正则段）**
   - 把 `ss = re.sub(...)` 那一串（标点后补空格、删标点前空格、中英引号/逗号互转、`Dr.` 特例、`-`/`%` 空格规则、`.·`结尾）抽成语义化规则写进 `rules/cleaning_rules.yaml`，供 `cleaner/` 现有规则引擎直接跑，无新依赖。

4. **在 cleaner 中新增"重复段落清洗器"（抄 _remove_duplicate_subtitle）**
   - 用 Levenshtein 相似度阈值（复用其 `thresholdTextSimilarity` 可配置思想，默认 0.9）做两遍：
     - 第一遍：连续窗口合并（原算法），应对视频反复播放同一字幕的瞬时重复；
     - 第二遍：全文档跨区重复检测（相似行仅保留首现，这是对原算法的增强），应对"同一段在不同时间重复出现"。
   - 注意保留我们 P1 的分句结构：去重按"块/句"为原子单位，勿先拍扁再查重。

5. **NFKC 归一化前置**
   - 抄其"合并前先 `unicodedata.normalize('NFKC')`"的顺序：先归一化→再去重→再纠错，避免因全角/半角差异漏掉重复。

**结论一句话**：这个仓库不值得我们整体接入，但 `reformat.py` 的词级纠错配方 + typoMap 配置驱动机制 + Levenshtein 去重算法，值得我们打 2 个轻量补丁进 `cleaner/` 和 `rules/cleaning_rules.yaml`，正好啃下问题 2 和问题 4。
