# 待改进清单 — 清洗工作流（暂存，后续按优先级执行）

> **版本**: v1.0 · **状态**: 当前待办 · 更新于 2026-08-11
> **暂存日期**: 2026-08-10 · **来源**: 用户要求先暂存，后续再实施
> **背景**: 清洗工作流核心已完成（A1-D2 + 验收），以下为审视出的改进空间
> **状态**: 🔴 A/G/I 已于 2026-08-10 完成（地基加固），其余暂缓

## 完成项 ✅

| # | 改进 | 说明 | 状态 |
|---|------|------|------|
| **A** | 残留检测器补全 | `scan_residual` 扩展覆盖水印/推荐问题/栏目/孤立答主名（与清洗删除类型对齐） | ✅ 完成 |
| **G** | 规则文件 schema 校验 | `cleaning_rules.yaml` 加载时用 jsonschema 校验，格式错明确报错（防静默失效） | ✅ 完成 |
| **I** | 清洗质量自动验证 | `check_content_integrity` 检查正文误删（保留率/中文内容/空壳），挂进 clean_kb | ✅ 完成 |
| **D** | 增量清洗（阶段0 修复） | F3 规则指纹路径 bug 修复（`config/`→`rules/`），恢复"规则变更触发重洗"机制 | ✅ 阶段0完成 |
| **H** | 输出接口/结构化输出（阶段0 原型） | `clean_asr_json.py` 产出保结构 `_clean.json`（接口契约落地）；正式并入 `block_pipeline.py` 待阶段1 | 🔶 原型完成，正式化待阶段1 |
| **E** | 水印识别更准（阶段3 转写内容保护） | v0.5.0 新增 `_is_teaching_line`（教学白名单）+ 水印删除（时间戳/UI词）+ 乱码检测 `_is_garbled` | ✅ v0.5.0完成 |

## 待办（暂缓）

| # | 改进 | 说明 | 优先级 |
|---|------|------|--------|
| **C** | 爬虫集成 | 目前"爬完再手动 clean_kb"；挂进 crawl_all.py 实现"爬完自动清洗" | 🟡 中 |
| **J** | 可观测性/日志 | 清洗失败/规则变更历史无持久化；用 logging | 🟡 中 |
| **B** | 多站点规则分组 | 只有 zhihu 分组；用户后续爬 juejin/csdn 等需各建分组 | 🟢 依赖数据 |
| **F** | 测试覆盖扩展 | _acceptance.py 10 项偏窄；加边界/规则回归测试（已补 10 轮验证脚本 `_tools/_verify_10rounds.py` + 教学门禁 `test_teaching_retention.py`） | 🟢 低 |

## 结论（讨论共识）

- 完整清单 = 10 项（A-J），其中 G/H/I/J（规则校验/输出接口/质量验证/日志）是**工程根因**
- **不需要去 GitHub 找**新算法——改进全是"工程完善"（检测/校验/接口/日志），用现有工具（jsonschema 校验 YAML、logging 做日志）自研即可
- 唯一可能借鉴 GitHub 的：E（中文 NLP 方法）、B（多站点噪音参考），但依赖未来数据，不急
