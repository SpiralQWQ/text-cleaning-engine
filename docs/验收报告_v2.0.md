# 清洗工作流验收报告 · 阶段 0 接口适配

> **版本**: v2.0 · **状态**: 历史验收 · **日期**: 2026-08-11
> **范围**: 阶段 0 接口适配（上游转写工具 转写产出 → 清洗项目接入规范落地）
> **配套**: `docs/接口对接/接口对接报告.md` + 示例文件 · `docs/补丁重构计划_v1.1.md`
> **验收方式**: 每个最小单元 task（T1-T7）完成后本体审核（完成度/预期/改动范围/隐藏bug/硬编码/融合度），全部通过后整体验收

---

## 一、验收范围

本次阶段 0 覆盖 7 个最小单元 task（T1-T7），围绕接口契约落地：

| task | 内容 | 对应接口需求 |
|------|------|------------|
| T1 | 修 F3 规则指纹路径 bug | 恢复增量失效机制 |
| T2 | 分析接口示例噪音类型 | 摸清 visual/json 全部噪音 |
| T3 | video_ocr 水印词入 YAML | 短样本水印漏删修复 |
| T4 | 接口示例 visual 清洗验证 | 水印删/描述留/教学保留 |
| T5 | json 逐段清洗原型（保结构） | `_clean.json` 产出（接口核心） |
| T6 | `_clean.json` 边界验证 | 空段/短段/纯标点/重播 |
| T7 | 接口全流程回归 + 验收不破坏 | 红线全守 + 原功能不坏 |

## 二、全 task 完整性检查

| # | task | 状态 | 本体审核结论 |
|---|------|------|------------|
| T1 | 修 F3 规则指纹路径 bug | ✅ | `config/` → `rules/`，指纹返回真实 md5，规则变化触发重洗 |
| T2 | 分析接口示例噪音类型 | ✅ | 识别 visual 水印词 + 教学短行；json 重复段/标点乱码/review |
| T3 | video_ocr 水印词入 YAML | ✅ | 8 条行首正则，水印删/教学留/描述不误删 |
| T4 | 接口示例 visual 清洗验证 | ✅ | 独立行水印全删，GLM 描述保留，教学短行保留 |
| T5 | json 逐段清洗原型 | ✅ | `clean_asr_json.py` 产出保结构 `_clean.json` |
| T6 | `_clean.json` 边界验证 | ✅ | 6 边界全过（空json/中文段/纯标点/短段/重播/review） |
| T7 | 接口全流程回归 | ✅ | 红线 15/15 + 原 14 项验收全过 |

**完整性**: 7/7 全部完成并审核通过。

## 三、是否达到预期

| 接口契约要求 | 结果 |
|-------------|------|
| json 逐段/逐句清洗，保留 segments/sentences 结构 | ✅ `_clean.json` 保结构输出 |
| start_ms/end_ms/confidence/review 不改值 | ✅ 红线回归 15/15 验证 |
| 中文讲解段永不删 | ✅ 示例中文段全保留 |
| 同文本+同时间戳=重复记录删后留前 | ✅ segments 6→5（删重复记录） |
| 同文本+不同时间戳=重播保留 | ✅ 边界5 验证 3 段全保留 |
| visual 水印独立行删除 | ✅ 坚持打卡/片名/知识点等全删 |
| GLM 描述保留（不误删） | ✅ 4 帧描述全保留 |
| 教学短行保留（问题3 边界） | ✅ MILK/Dishuching/Lavndey/语的 保留 |
| srt 不清洗 | ✅ 原型不碰 srt |
| 短样本水印漏删修复（F4） | ✅ 行首正则解决 |

## 四、改动文件清单

| 文件 | 改动 | 类型 |
|------|------|------|
| `cli/clean_batch.py` | `config/` → `rules/`（1 行） | 修复 bug |
| `rules/cleaning_rules.yaml` | 新增 8 条 video 水印正则 + 注释更新 | 规则扩展 |
| `cleaner/clean_asr_json.py` | **新增**：ASR json 逐段清洗原型 | 新功能 |
| `docs/接口对接/` | 接口报告 + 示例文件（复制自 上游转写工具） | 文档 |

**未改动**：`cleaner/cleaning.py`、`tests/test_acceptance.py`、其他全部——**原 14 项验收测试全过，无回归**。

## 五、漏洞/bug 扫描

| 检查项 | 结果 |
|--------|------|
| 新增脚本语法编译 | ✅ 正常 |
| 硬编码绝对路径 | ✅ 无（原型用相对路径/参数） |
| 未用导入 | ✅ 无 |
| 规则 schema 校验 | ✅ 通过（YAML 正常加载） |
| 中文/英文编码 | ✅ UTF-8 全程 |
| 空输入/缺失字段 | ✅ 安全降级 |
| 纯标点段 | ✅ 清空（边界3） |
| 时间戳重叠段（数据缺陷） | ✅ 不误处理（文本匹配定位） |
| 与原功能冲突 | ✅ 14 项验收全过 |

**过程中发现并修复**：
1. T3 正则 `纯英[文宇]字幕` 漏 `纯英文宇幕`（OCR 错别字）→ 改 `纯英[文宇字]+幕$`
2. T5 纯标点段残留 `'. . ?'` → 加"无实质字符清空"逻辑
3. 三轮对抗验证（2026-08-11）：`clean_asr_json` 对 `None`/字符串/`segments=None`/非list 输入崩溃 → 加输入类型防御（`_dedup_segments` 跳过非 dict 段）；10/10 对抗场景全过，9/9 集成场景全过，原 14 项验收零回归
4. **10 轮深度验证**（2026-08-11，`_verify_10rounds.py`）：**295/295 通过**。10 轮攻击角度（功能/边界/畸形fuzz/Unicode/重复稳定/性能/集成/红线/规则一致/综合压力）。fuzz 抓出 2 个隐藏 bug → 修复：
   - `_dedup_segments` 段 text 为 None → `_segment_key(None)` 崩溃 → 改 `seg.get("text") or ""`
   - `_clean_text_segment(None)` → `_norm_punct` 崩溃 → 入口加 `if text is None: return ""`

## 六、遗留说明

| 项 | 说明 |
|----|------|
| 词级纠错（S tla r） | 接口定案：Claude 笔记阶段纠正，清洗层不强行改词 |
| GLM 描述内嵌水印 | 接口定案：保留（描述是有效内容） |
| 长视频分块 | 接口定案：上游转写工具 笔记阶段处理（清洗层不管） |
| 段级去重 | 原型已实现"同文本同时间戳"去重；"跨段近重复(相似度)"待阶段2 R3 |
| 结构化重构 | `clean_asr_json.py` 是**原型**，正式并入 `block_pipeline.py` 待阶段1 R1 |

## 七、验收结论

**✅ 通过**。阶段 0 接口适配全部达成：接口契约红线 15/15 遵守，7 个最小单元 task 逐个本体审核通过，`_clean.json` 原型成功产出保结构输出，原 14 项验收测试零回归。**清洗工作流现已能对接 上游转写工具 转写产出，产出符合接口契约的 `_clean.json` + `_clean_visual.txt`**。

---

*验收方式说明: 每个 task 完成后由本体(Claude)执行测试审核(完成度/预期/改动范围/隐藏bug/硬编码/融合度), 通过后进入下一 task; 本次为阶段 0 整体验收。*
